_target_: lightning.pytorch.trainer.Trainer

default_root_dir: ${paths.experiment_dir}

min_epochs: 1
max_epochs: 10

accelerator: gpu
devices: 1

precision: 32

check_val_every_n_epoch: 1

deterministic: false

log_every_n_steps: 1
